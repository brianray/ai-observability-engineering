"""Runnable examples, one module per chapter.

Every example is registered with :mod:`chapters.registry`, which means it
is discoverable by the simulator (``aiobs-sim run --all``) and covered by
the functional test suite whether or not anyone wrote a bespoke test for
it. Adding an example to the book means adding it here; nothing else has
to change.

See ``docs/AUTHORING.md``.
"""
